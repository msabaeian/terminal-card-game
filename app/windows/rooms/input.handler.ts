const handleRoomsInputs = (key) => {
    switch (key) {
        case "C":
            // create room
            break;
        default:
            break;
    }
};

export default handleRoomsInputs;
